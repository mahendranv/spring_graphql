server:
  port : 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/books?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: book_admin
    password: password@123

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect